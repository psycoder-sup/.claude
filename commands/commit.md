# Commit Changes

summarize code changes with uncomitted changes and create commits. Write commit message consie and simple.
Single commit should responsible to single change scope. If there are two or more major differences, separate commits.
Use @agent-git-workflow-manager.